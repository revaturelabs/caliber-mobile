import Enzyme from 'enzyme';
import React from 'react';
import 'react-native';
import 'jest-enzyme';
import '@testing-library/jest-dom';
import { ReportSelector } from '../../reports/ReportSelector';

describe('Tests for report selection tool', () => {
    test('That when a year is selected, reports are shown for that year.', () => {

    });

    test('That when a report type is selected, that type of report is displayed.', () => {

    });

    test('That when a trainee is selected, report only shows that trainee.', () => {

    });

    test('That when overall is selected, the overall trainee report is displayed.', () => {

    });

    test('That when a week is selected, only the report for that week is displayed.', () => {

    });

    test('That when all weeks is selected, the report for all weeks is displayed.', () => {
        
    })
})